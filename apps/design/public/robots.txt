# Nebutra Design - Live design system surface
# Internal acceptance surface for @nebutra/ui + @nebutra/tokens - do not index
#
# Promotion to indexable requires adopting apps/landing/src/lib/seo first
# (route registry + buildPageMetadata + sharded sitemap) so this origin does not
# hand-roll another robots/canonical/hreflang dialect.

User-agent: *
Disallow: /

# No sitemap - not a public surface
