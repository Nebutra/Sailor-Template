{
  "name": "mail-preview",
  "version": "0.0.0",
  "private": true,
  "description": "Local email preview and export surface for @nebutra/email",
  "type": "module",
  "scripts": {
    "check": "tsx scripts/check.ts",
    "export": "tsx scripts/export.ts",
    "render:react": "tsx scripts/render-react-templates.ts",
    "clean": "rm -rf .react-email .turbo"
  },
  "dependencies": {
    "@nebutra/email": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
