# DNS inventory (brand-dogfooded)

Hosts from `brand.domains`. Infra shape in `topology.defaults.yaml`.
`pnpm dns:render` writes gitignored zone files. See docs/architecture/brand-hardcode-governance.md.
