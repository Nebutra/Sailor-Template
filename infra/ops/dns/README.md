# DNS inventory (brand-dogfooded)

Hosts from `brand.domains`. Infra shape in `topology.defaults.yaml`.
`pnpm dns:render` writes gitignored zone files. See docs/design-system/brand-hardcode-governance.md.
