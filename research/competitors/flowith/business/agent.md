# Agent — Flowith "Neo"

**Entry (O):** the Neo Agent chip on the home/in-flow composer: placeholder "Describe a complex task for the Agent to handle...", model slot "Auto · Neo Agent" (Auto = let Neo decide output format; Text/Image/Video/Slides/Website selectable), MAX toggle. Inside Image mode an "Agent off" toggle exists, i.e. the agent can also wrap a single-modality generation (O).

**Plan display (O — key finding):** projected on the canvas as nodes. In the owner's completed run: prompt → step-label node → **Group 1** (six parallel research nodes) → ten sequential step-label nodes each followed by its output document node (with version strings) → a Sandbox Preview node for the generated website. Badge "Powered by Neo". No side panel, task list, or timeline.

**Controls on the run (O):** each step-label node has Rerun · Edit · Delete; output nodes are normal nodes (quotable, Follow Up, colour tags). Group has an editable name.

**Approval / interruption (U):** no approval gate or cancel affordance observed (run was complete; no credits to start one). Whether the user can keep editing while Neo runs: U.

**Knowledge (O/I2):** Knowledge Garden (account-level knowledge bases) is the persistent context object; the community "Knowledge Market" and an "oracle" tab suggest gardens are the reusable identity Flowith sells, not characters or styles (I2).
