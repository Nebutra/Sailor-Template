# flowith.library — /flows

Flat, account-level list of flows ('All Flow') with name search, sort and two view toggles; identical population to the sidebar History. Projects exist as a sidebar group but were empty and not opened (U). Evidence: `evidence/library.default.webp`.
