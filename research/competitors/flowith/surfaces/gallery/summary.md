# flowith.gallery — /gallery (Media History)

Account-scoped media library organised by day (date strip + date menu), tabs Generations | Uploads, Search File, Starred, Type filter. The in-flow dock reuses the same control set scoped to one flow (I2). Evidence: `evidence/media-history.default.webp`.
