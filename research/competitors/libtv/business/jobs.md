# Jobs

- **O** Generation is initiated per node (▶) or by the Agent; the only job-centre surface is `生成历史` (canvas dock): scope 本画布 / 全部画布, counters 图片 / 视频 / 音频, 按成员筛选, 所有评级, 时间倒序, 批量操作, thumbnail size slider. No global queue/notification centre beyond browser notifications (`开启浏览器通知` prompt in the Agent drawer, `通知设置` in Agent 设置).
- **O** Cost is surfaced up front: 积分 balance in the header; `/pretrain` computes 预计消耗积分 after upload; `/comfy` shows a credit estimate (10); Agent has 积分预算管理 with an alert threshold.
- **O** Batch: 生成数量 1/2/4 on the video node, 图片张数 1–4 on liblib.art (VIP for 4).
- **I2** Jobs are server-side (history is per-project and member-attributed), so they should survive navigation/reload; not verified — no generation was run (credits 0, and an Aliyun CAPTCHA is mounted on liblib.art generator pages).
- **U** Queue position, progress UI, cancel, retry, failure states.
