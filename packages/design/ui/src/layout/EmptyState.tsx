/**
 * EmptyState lives in `@nebutra/ui/primitives` (the simple form and the
 * compound Root / Icon are one symbol there); this path re-exports it.
 */
export { EmptyState, type EmptyStateProps } from "../primitives/empty-state";
