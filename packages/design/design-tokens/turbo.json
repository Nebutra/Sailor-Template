{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["tokens/**", "style-dictionary.config.mjs", "tsconfig.json", "package.json"],
      "cache": false,
      "outputs": ["build/**"]
    }
  }
}
