{
  "name": "@nebutra/notifications",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "nebutra": {
    "status": "foundation",
    "productionReady": false,
    "requires": [
      "NOVU_API_KEY + NOVU_APPLICATION_IDENTIFIER (managed)",
      "or direct dispatchers (email/sms/push) for custom"
    ],
    "gaps": [
      "Novu provider adapter stubs — no batching, retries limited",
      "In-app feed storage not implemented",
      "User preference schema pending"
    ]
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./novu": "./src/providers/novu.ts",
    "./direct": "./src/providers/direct.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nebutra/logger": "workspace:*",
    "@novu/node": "^2.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
