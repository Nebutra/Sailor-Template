// Generated by create-sailor. Edit freely.
import { createProviderRegistry } from 'ai';
// @sailor:provider:openai
import { openai } from '@ai-sdk/openai';
// @sailor:provider:anthropic
import { anthropic } from '@ai-sdk/anthropic';
// @sailor:provider:google
import { google } from '@ai-sdk/google';
// @sailor:cn-compatible
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';

// @sailor:cn-instance:siliconflow
const siliconflow = createOpenAICompatible({
  name: 'siliconflow',
  baseURL: process.env.SILICONFLOW_BASE_URL ?? 'https://api.siliconflow.cn/v1',
  apiKey: process.env.SILICONFLOW_API_KEY!,
});

// @sailor:cn-instance:bailian
const bailian = createOpenAICompatible({
  name: 'bailian',
  baseURL: process.env.DASHSCOPE_BASE_URL ?? 'https://dashscope.aliyuncs.com/compatible-mode/v1',
  apiKey: process.env.DASHSCOPE_API_KEY!,
});

// @sailor:custom-endpoint
const custom = createOpenAICompatible({
  name: process.env.CUSTOM_AI_NAME ?? 'custom',
  baseURL: process.env.CUSTOM_AI_BASE_URL!,
  apiKey: process.env.CUSTOM_AI_API_KEY!,
});

export const registry = createProviderRegistry({
  // @sailor:registry-entries
  openai,
  anthropic,
  google,
  siliconflow,
  bailian,
  custom,
});

export type ProviderId = keyof typeof registry;
