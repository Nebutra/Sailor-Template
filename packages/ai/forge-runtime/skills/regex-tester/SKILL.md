---
name: regex-tester
description: 正则测试
version: 1.0.0
allowed_tools: []
mcp_servers: []
---

## What this skill does

正则测试

## How to invoke

```http
POST /api/v1/tools/dev/regex-tester/invoke
{"input":{}}
```

MCP tool name: `dev__regex-tester`

## Engine

See registry metadata for `engine.upstream` on tool `dev/regex-tester`.
