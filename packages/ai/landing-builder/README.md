# @nebutra/landing-builder

BrandContext-first landing page Play package.

This package owns Play orchestration and static site artifacts only. It does not
own sandbox execution, deploy credentials, BrandContext, or agent runtime state.

```bash
pnpm landing-builder:doctor
pnpm landing-builder:quickstart "AI debugging for indie devs"
pnpm landing-builder:debug
```
