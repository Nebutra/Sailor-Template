{
  "name": "@nebutra/code-index",
  "version": "0.2.3",
  "private": false,
  "license": "MIT",
  "type": "module",
  "nebutra": {
    "graph": "labs",
    "status": "wip",
    "productionReady": false,
    "requires": [
      "An Embedder adapter (OpenAI / Ollama / Bedrock / OpenAI-compatible / …)",
      "A VectorStore adapter (local on-disk or hosted)",
      "@nebutra/tenant for tenant scoping (collection key = tenantId + project)"
    ],
    "gaps": [
      "Ships port contracts + a pure structural/line/markdown chunker + the incremental index engine; concrete embedder/vector-store adapters are injected, not bundled",
      "Structural chunking uses an injected CodeParser port — no native tree-sitter dependency in this package"
    ],
    "featureId": "code-index",
    "category": "ai",
    "surface": "semantic-index",
    "summary": "Multi-tenant codebase semantic-index grammar: deterministic content-hash chunking (structural / line / markdown), incremental scan, embedding-profile drift recreate, cosine retrieval — all behind provider-agnostic Embedder/VectorStore ports"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nebutra/ai-primitives": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.0.18"
  },
  "homepage": "https://github.com/Nebutra/Nebutra-Sailor/tree/main/packages/ai/code-index#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nebutra/Nebutra-Sailor.git",
    "directory": "packages/ai/code-index"
  },
  "bugs": {
    "url": "https://github.com/Nebutra/Nebutra-Sailor/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ]
}
