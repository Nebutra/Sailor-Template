# @nebutra/onboarding

> Status: Foundation — client-side flow works; server-side completion sync + analytics hook pending.

Onboarding flow primitives for first-run user setup. Tracks step completion
in `localStorage` so users resume where they left off across reloads.

## Pending work

- Server-side completion sync (so progress survives device switches)
- Analytics hook for step transitions
